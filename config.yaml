# Pi 4-Pillar Agent Helper — Ecosystem Config
agent_name: zoid
agent_id: zoid-agent-001
pillar_reference: 4-pillar     # Aiman / Aigent / Aidration / Aimotions
monitor_target: agent-runtime  # Monitors the active agent in Hermes or Pi harness
inference_backend: bionic      # Reuses LM Studio / Bionic on port 36093 (not new layer)
mesh_topic: zoid/focus
focus_threshold: 0.5           # Drift repair trigger
metrics_port: 9090
session_dir: ./pi-sessions
